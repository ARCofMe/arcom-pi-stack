DISPATCHER_BACKEND_CMD=python -m http.server ${DISPATCHER_BACKEND_PORT:-8000}
DISPATCHER_FRONTEND_CMD=npm run dev -- --host 0.0.0.0 --port ${DISPATCHER_FRONTEND_PORT:-4173}

# TODO: add real dispatcher backend environment variables here.
